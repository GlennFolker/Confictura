package confictura.entities.effect;

import arc.func.*;
import mindustry.entities.*;

public class CEffect extends Effect{
    public CEffect(float lifetime, float clip, Cons<EffectContainer> cons){
        super(lifetime, clip, cons);
    }
}
